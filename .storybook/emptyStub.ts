/**
 * Empty stub for Storybook/Vite.
 * Used for Node built-ins that incyclist-services imports but never meaningfully
 * calls in the UI paths exercised by stories (fs, path, net, os, timers, stream).
 * Mirrors the empty-module pattern used in metro.config.js.
 */
export default {};
//module.exports = {};
